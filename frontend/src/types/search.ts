export interface SearchResult {
    id: number,
    title: string | null,
    artist: string | null,
    album: string | null,
    originalLink: string | null,
    image: string | null,
    audioTime: number,
}
